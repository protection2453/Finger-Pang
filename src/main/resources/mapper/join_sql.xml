<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="join">
	<select id="selectNickname" resultType="string">
		SELECT user_nickname FROM user LIMIT 1
    </select>
    <select id="selectId" resultType="string">
            SELECT user_id FROM user LIMIT 1
    </select>
    <select id="selectAll" resultType="com.example.test.db.dto.UserDTO">
            SELECT * FROM user LIMIT 1
    </select>
    <insert id="insertUser">
        INSERT INTO user(user_id, user_password, user_nickname, user_phone, user_gender, user_job, 
        user_area, user_age, user_religion, user_hobby, user_pet, user_marry, user_grade_point, user_credit)
        values(#{user_id}, #{user_password}, #{user_nickname}, #{user_phone}, #{user_gender}, #{user_job}, 
        #{user_area}, #{user_age}, #{user_religion}, #{user_hobby}, #{user_pet}, #{user_marry}, #{user_grade_point}, #{user_credit})
    </insert>
</mapper>